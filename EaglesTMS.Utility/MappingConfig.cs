

namespace EaglesTMS.Utility
{
    public class MappingConfig : Profile
    {
        public MappingConfig()
        {
            //private readonly IMapper _mapper;
            //IMapper _mapper
            //apiResponse.Result = _mapper.Map<List<DepartmentDTO>>(Departments);
            // CreateMap<WeatherForecast, WeatherForecastViewModel>()
            //.ForMember(dest => dest.SummaryText, input => input.MapFrom(i => i.Summary))
            //.ReverseMap();

            //CreateMap<ApplicationUser, UpsertUserVM>();
        }
    }
}
